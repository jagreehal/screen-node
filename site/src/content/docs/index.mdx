---
title: sandbox
description: A supply-chain guard for npm/pnpm/yarn/bun. sandbox add vets a package before it installs (malware, typosquats, fresh-release worm window), then installs mode-aware so your IDE gets host-native binaries by default. Use explicit sandbox <pm> when you want the throwaway-container boundary. Use check to vet without installing.
template: splash
pagefind: false
hero:
  tagline: Catch a bad dependency before it lands. sandbox add vets every dependency, then installs mode-aware. One mode per project.
---

import Landing from '../../components/Landing.astro';

<Landing />
